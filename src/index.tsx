import App from 'app';
import React from 'react';
import ReactDOM from 'react-dom';
import './config/i18n';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
